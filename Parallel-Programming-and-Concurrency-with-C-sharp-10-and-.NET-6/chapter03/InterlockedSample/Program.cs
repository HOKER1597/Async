// Import the InterlockedSample namespace.
using InterlockedSample; 
// Print "Hello, World!" to the console.
Console.WriteLine("Hello, World!"); 
// Create a new instance of the InterlockedExample class.
var interlocked = new InterlockedExample(); 
// Call the PerformCalculations method on the InterlockedExample instance.
interlocked.PerformCalculations(); 
// Wait for the user to press a key before exiting the application.
Console.ReadLine();
